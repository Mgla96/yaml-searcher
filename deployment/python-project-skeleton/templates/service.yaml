apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-project-skeleton.fullname" . }}
  labels:
    {{- include "python-project-skeleton.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-project-skeleton.selectorLabels" . | nindent 4 }}
